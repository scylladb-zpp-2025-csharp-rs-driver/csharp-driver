name: CI

permissions:
  contents: read

on:
  push:
    branches:
      - master
    paths-ignore:
      - "docs/**"
      - .github/workflows/docs-pages.yml
      - .github/workflows/docs-pr.yml
      - .github/workflows/publish.yml
      - .gitignore
      - "*.md"
      - .gitignore
      - .gitattributes
      - "*.md"
      - "*.json"
      - NOTICE
      - doxyfile
      - .nav
  pull_request:
    types: [opened, synchronize, reopened]
    paths-ignore:
      - "docs/**"
      - .github/workflows/docs-pages.yml
      - .github/workflows/docs-pr.yml
      - .github/workflows/publish.yml
      - .gitignore
      - .gitattributes
      - "*.md"
      - "*.json"
      - NOTICE
      - doxyfile
      - .nav
jobs:
  linting-and-basic-unit-tests:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - name: Setup .NET
        uses: actions/setup-dotnet@26b0ec14cb23fa6904739307f278c14f94c95bf1 # v5
        with:
          dotnet-version: '9.0.x'

      - name: Run static checks (checks formatting, linting and performs code analysis)
        run: make check

      # # The Rust unit tests and doctests previously ran nowhere in CI, so the FFI layer's own tests -
      # # including the compile_fail doctests that prove its borrow-checker invariants - could rot
      # # unnoticed.
      # - name: Run Rust unit tests and doctests
      #   run: make test-rust

      # # The test-only FFI exports must not be reachable from a shippable build.
      # - name: Check the default build exports no test-only symbols
      #   run: make check-no-test-exports

      - name: Copy development signing key
        run: "cp -f build/scylladb-dev.snk build/scylladb.snk"

      - name: Build the project
        run: dotnet build src/Cassandra/Cassandra.csproj

      - name: Run unit tests
        run: TARGET_FRAMEWORK=net9 make test-unit

  #     # Same suite under a hostile GC (tiny gen0, frequent compaction, no tiered JIT). Any managed
  #     # buffer handed to Rust without being pinned is far likelier to be caught here.
  #     - name: Run unit tests under GC stress
  #       run: TARGET_FRAMEWORK=net9 make test-unit-gcstress

  # sanitizers:
  #   runs-on: ubuntu-24.04
  #   steps:
  #     - name: Checkout repository
  #       uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

  #     - name: Install the nightly toolchain
  #       run: rustup toolchain install nightly --component rust-src,miri

  #     # Runs first, deliberately. It asserts that ASAN reports two real defects planted for the
  #     # purpose, so a green sanitizer run below cannot be confused with the sanitizer being disabled.
  #     - name: Check the sanitizer is armed
  #       run: make test-rust-asan-selftest

  #     - name: Run Rust unit tests under AddressSanitizer and LeakSanitizer
  #       run: make test-rust-asan

  #     # Catches the aliasing and pointer-provenance violations ASAN structurally cannot see.
  #     - name: Run Rust unit tests under Miri
  #       run: make test-rust-miri

  integration-tests:
    needs: linting-and-basic-unit-tests
    runs-on: ubuntu-24.04
    strategy:
      matrix:
        include:
          - scylla-version: LTS-LATEST
            target-framework: net9
            dotnet-version: '9.0.x'
          - scylla-version: LTS-PRIOR
            target-framework: net8
            dotnet-version: '8.0.x'
          - scylla-version: LATEST
            target-framework: net8
            dotnet-version: '8.0.x'
          - scylla-version: PRIOR
            target-framework: net9
            dotnet-version: '9.0.x'
      fail-fast: false

    env:
      TARGET_FRAMEWORK: ${{ matrix.target-framework }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6

      - name: Setup .NET ${{ matrix.dotnet-version }}
        uses: actions/setup-dotnet@26b0ec14cb23fa6904739307f278c14f94c95bf1 # v5
        with:
          dotnet-version: ${{ matrix.dotnet-version }}

      - name: Set up JDK 8
        uses: actions/setup-java@0f481fcb613427c0f801b606911222b5b6f3083a # v5
        with:
          java-version: 8
          distribution: 'temurin'

      - name: Setup Python 3
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: '3.11'

      - name: Install get-version CLI
        run: |
          git clone https://github.com/scylladb-actions/get-version.git
          cd get-version
          go mod tidy
          go build -o get-version

      - name: Get scylla version
        id: scylla-version
        run: |
          cd get-version
          if [[ "${{ matrix.scylla-version }}" == "LTS-LATEST" ]]; then
            echo "SCYLLA_VERSION=release:$(./get-version --source dockerhub-imagetag --repo scylladb/scylla -filters "^[0-9]{4}$.^[0-9]+$.^[0-9]+$ and LAST.1.LAST" | tr -d '\"')" >> $GITHUB_ENV
          elif [[ "${{ matrix.scylla-version }}" == "LTS-PRIOR" ]]; then
            echo "SCYLLA_VERSION=release:$(./get-version --source dockerhub-imagetag --repo scylladb/scylla -filters "^[0-9]{4}$.^[0-9]+$.^[0-9]+$ and LAST-1.1.LAST" | tr -d '\"')" >> $GITHUB_ENV
          elif [[ "${{ matrix.scylla-version }}" == "LATEST" ]]; then
            echo "SCYLLA_VERSION=release:$(./get-version --source dockerhub-imagetag --repo scylladb/scylla -filters "^[0-9]{4}$.^[0-9]+$.^[0-9]+$ and LAST.LAST.LAST" | tr -d '\"')" >> $GITHUB_ENV
          elif [[ "${{ matrix.scylla-version }}" == "PRIOR" ]]; then
            SCYLLA_VERSION=$(./get-version --source dockerhub-imagetag --repo scylladb/scylla -filters "^[0-9]{4}$.^[0-9]+$.^[0-9]+$ and LAST.LAST.LAST-1" | tr -d '\"')
            if [[ -z "$SCYLLA_VERSION" ]]; then
              echo "No PRIOR version found, skipping test"
              echo "SKIP_TEST=true" >> $GITHUB_ENV
            else
              echo "SCYLLA_VERSION=release:$SCYLLA_VERSION" >> $GITHUB_ENV
            fi
          elif echo "${{ matrix.scylla-version }}" | grep -P '^[0-9\.]+'; then # If you want to run specific version do just that
            echo "SCYLLA_VERSION=release:${{ matrix.scylla-version }}" >> $GITHUB_ENV
          else
            echo "Unknown scylla version name `${{ matrix.scylla-version }}`"
            exit 1
          fi

      - name: Run unit tests
        run: make test-unit

      - name: Run logging tests
        run: make test-logging

      - name: Run integration tests on Scylla
        if: env.SKIP_TEST != 'true'
        run: make test-integration-scylla

  integration-tests-simulacron:
    needs: linting-and-basic-unit-tests
    runs-on: ${{ matrix.version }}
    strategy:
      matrix:
        include:
          - version: ubuntu-24.04-arm
          - version: windows-2022
          - version: windows-11-arm
          - version: macos-15-intel
          - version: macos-14

    env:
      TARGET_FRAMEWORK: net9

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Setup .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: '9.0.x'

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Python 3
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Configure Simulacron loopback aliases
        if: startsWith(matrix.version, 'macos-')
        run: |
          for sub in {0..4}; do
            echo "Opening for 127.0.$sub"
            for i in {0..255}; do sudo ifconfig lo0 alias 127.0.$sub.$i up; done
          done

      - name: Run unit tests
        run: make test-unit

      - name: Run integration tests without CCM (Simulacron)
        run: make test-integration-simulacron

